@model IEnumerable<Music.Models.Artist>

@{
    ViewBag.Title = "Index";
}

<h2>Artist</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.ActionLink(item.Name, "BrowseArtist", "Albums", new { id = item.ArtistID }, null)
        </td>

    </tr>
}

</table>


